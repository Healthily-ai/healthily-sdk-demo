diff --git a/lib/commonjs/web/utils/prepare.js b/lib/commonjs/web/utils/prepare.js
index d57e7c86ad2bc0f72f74673214f57e3f5a55c209..55b04865aeff4b6f55e55ee83d3086239410d3f1 100644
--- a/lib/commonjs/web/utils/prepare.js
+++ b/lib/commonjs/web/utils/prepare.js
@@ -46,9 +46,9 @@ const prepare = (self, props = self.props) => {
     ...rest
   };
   if (origin != null) {
-    clean['transform-origin'] = origin.toString().replace(',', ' ');
+    clean.transformOrigin = origin.toString().replace(',', ' ');
   } else if (originX != null || originY != null) {
-    clean['transform-origin'] = `${originX || 0} ${originY || 0}`;
+    clean.transformOrigin = `${originX || 0} ${originY || 0}`;
   }
 
   // we do it like this because setting transform as undefined causes error in web
diff --git a/lib/module/web/utils/prepare.js b/lib/module/web/utils/prepare.js
index 113eb2b45c518fb2f1dd4fdcca1a87b29572cfe7..d1d57241fd935344364aa3d97c4f3da95fcab01f 100644
--- a/lib/module/web/utils/prepare.js
+++ b/lib/module/web/utils/prepare.js
@@ -40,9 +40,9 @@ export const prepare = (self, props = self.props) => {
     ...rest
   };
   if (origin != null) {
-    clean['transform-origin'] = origin.toString().replace(',', ' ');
+    clean.transformOrigin = origin.toString().replace(',', ' ');
   } else if (originX != null || originY != null) {
-    clean['transform-origin'] = `${originX || 0} ${originY || 0}`;
+    clean.transformOrigin = `${originX || 0} ${originY || 0}`;
   }
 
   // we do it like this because setting transform as undefined causes error in web
